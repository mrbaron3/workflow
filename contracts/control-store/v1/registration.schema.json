{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mrbaron3/servo/contracts/control-store/v1/registration.schema.json",
  "title": "Repository Registration v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "repository",
    "enabled",
    "issueMonitorEnabled",
    "prMonitorEnabled",
    "executionEnabled",
    "configuration",
    "version",
    "createdAt",
    "updatedAt"
  ],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "repository": {
      "type": "string",
      "pattern": "^[a-z0-9](?:[a-z0-9-]{0,37}[a-z0-9])?/(?!\\.{1,2}$)[a-z0-9_.-]{1,100}$"
    },
    "enabled": {
      "type": "boolean"
    },
    "issueMonitorEnabled": {
      "type": "boolean"
    },
    "prMonitorEnabled": {
      "type": "boolean"
    },
    "executionEnabled": {
      "type": "boolean"
    },
    "configuration": {
      "type": "object",
      "maxProperties": 0
    },
    "version": {
      "type": "integer",
      "minimum": 1
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    }
  }
}
