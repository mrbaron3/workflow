<!--
  ubiquitous-language for one bounded context (DOC_TAXONOMY view 1 — 言語). Lives at
  _system/<ctx>/ubiquitous-language.md. Additive only.
  - Fill <…> and delete this comment plus any unused rows.
  - LANG-<CTX>-NNN ids are unique and stable within the context — never renumber/reuse.
  - PER-CONTEXT: a term may mean something different in another context. Do not make one global
    glossary; name cross-context translation points (ACL) in context-map.md (when it exists).
  - This is the vocabulary every other doc in this context (domain/arch/data/spec) is written in.
-->

# Ubiquitous language — <context> context

| ID | Term | Meaning (used consistently everywhere in this context) |
| --- | --- | --- |
| LANG-<CTX>-NNN | <Term> | <one-line definition the whole context agrees on> |
